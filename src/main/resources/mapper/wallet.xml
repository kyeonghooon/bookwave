<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.bookwave.repository.interfaces.WalletRepository">

	<select id="findWalletByUserId" resultType="com.library.bookwave.repository.model.Wallet">
		select * from wallet_tb where user_id = #{userId} 
	</select>

	
</mapper>