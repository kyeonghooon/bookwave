<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.bookwave.repository.interfaces.AdminRepository">
	<insert id="insert">
		insert into user_tb(login_id, social_id, password, name, role, subscribe, wave, mileage, status)
		values (#{loginId}, #{socialId}, #{password}, #{name}, #{role}, #{subscribe}, #{wave}, #{mileage}, #{status})
	</insert>
	
	<select id="readAllUser" resultType="com.library.bookwave.repository.model.User">
		select * from user_tb
	</select>
</mapper>
